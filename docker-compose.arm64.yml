# Quantus Telemetry - Docker Compose Configuration
# For ARM64 / Apple Silicon (M1/M2/M3) systems
# Frontend: http://localhost:3000
# Backend Core: ws://localhost:8000/feed
# Backend Shard: ws://localhost:8001/submit

services:
  telemetry-frontend:
    container_name: quantus-telemetry-frontend
    build:
      dockerfile: Dockerfile
      context: ./frontend/
    read_only: true
    tmpfs:
      - /var/cache/nginx:uid=101,gid=101
      - /var/run:uid=101,gid=101
      - /app/tmp:uid=101,gid=101
    environment:
      SUBSTRATE_TELEMETRY_URL: ws://localhost:8000/feed
    ports:
      - 3000:8000
    expose:
      - 3000
    restart: unless-stopped
    depends_on:
      - telemetry-backend-core

  telemetry-backend-shard:
    container_name: quantus-telemetry-shard
    platform: linux/amd64
    build:
      dockerfile: Dockerfile
      context: ./backend/
    read_only: true
    command: [
      'telemetry_shard',
      '--listen', '0.0.0.0:8001',
      '--core', 'http://telemetry-backend-core:8000/shard_submit'
      ]
    ports:
      - 8001:8001
    expose:
      - 8001
    restart: unless-stopped
    depends_on:
      - telemetry-backend-core

  telemetry-backend-core:
    container_name: quantus-telemetry-core
    platform: linux/amd64
    build:
      dockerfile: Dockerfile
      context: ./backend/
    read_only: true
    command: [
      'telemetry_core',
      '--listen', '0.0.0.0:8000'
      ]
    ports:
      - 8000:8000
    expose:
      - 8000
    restart: unless-stopped

